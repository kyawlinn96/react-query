import React from 'react';
import CloseIcon from '@/assets/cross.svg';

const CloseMainView = () => {
  return (
    <div>
      <img src={CloseIcon} alt='' className='w-5' />
    </div>
  );
};

export default CloseMainView;
